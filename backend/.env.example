# Copy this file to .env and fill in your values.
# The backend reads these at startup via require('dotenv').config().

# Required — get your key from https://aistudio.google.com/
GEMINI_API_KEY=api GEMINI_API_KEY

# Optional — defaults to gemini-2.0-flash if not set
GEMINI_MODEL=gemini-2.5-flash
